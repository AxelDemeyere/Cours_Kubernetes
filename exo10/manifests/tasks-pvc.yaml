apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: tasks-pvc
  namespace: fullstack

spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "128Mi"

